<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="top.haha233.smbms.dal.RoleMapper">
    <select id="queryAllRole" resultType="RoleBo">
        select
          id,
          roleCode,
          roleName
        from smbms_role;
    </select>
</mapper>